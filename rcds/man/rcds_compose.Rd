% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-compose.R
\name{rcds_compose}
\alias{rcds_compose}
\title{Compose a finished RCDS map layout}
\usage{
rcds_compose(
  main,
  locator = NULL,
  credits = NULL,
  title = NULL,
  subtitle = NULL,
  layout = c("poster", "stack", "sidebar"),
  canvas = "dark",
  base = 11,
  inset = c(0.62, 0.62, 0.98, 0.98)
)
}
\arguments{
\item{main}{The primary map (a ggplot).}

\item{locator}{Optional inset ggplot (e.g. from \code{\link[=rcds_locator]{rcds_locator()}}).}

\item{credits}{Optional credits strip (e.g. from \code{\link[=rcds_credits]{rcds_credits()}}).}

\item{title, subtitle}{Optional overall title/subtitle applied via
\code{patchwork::plot_annotation()}.}

\item{layout}{One of \code{"poster"} (locator top-right overlay, credits strip
bottom), \code{"stack"} (main over credits), or \code{"sidebar"} (locator + legend
column at right). Ignored if both \code{locator} and \code{credits} are \code{NULL}.}

\item{canvas}{Canvas key for the overall background.}

\item{base}{Base type size.}

\item{inset}{Numeric \code{c(left, bottom, right, top)} in npc for the locator
overlay when \code{layout = "poster"}.}
}
\value{
A patchwork object.
}
\description{
Assembles the archive's signature composition -- a main map, an optional
locator inset, and an optional credits strip -- into one balanced figure
using \code{patchwork}. Replaces the bespoke \code{plot_layout()}/\code{area()} wrangling
repeated across Days 2 and 16.
}
