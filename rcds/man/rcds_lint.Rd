% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-lint.R
\name{rcds_lint}
\alias{rcds_lint}
\title{Lint an R map script against the RCDS anti-patterns}
\usage{
rcds_lint(x, ignore = character())
}
\arguments{
\item{x}{A path to a \code{.R} file, or a character vector of code lines, or a
single string containing newlines.}

\item{ignore}{Character vector of rule codes (e.g. \code{c("A3", "A8")}) to skip.}
}
\value{
A data.frame of findings (\code{rule}, \code{severity}, \code{line}, \code{message},
\code{fix}), ordered by line. Zero rows means a clean bill of health. The frame
carries a \code{clean} attribute and prints a one-line summary via \code{\link[=message]{message()}}.
}
\description{
Static analysis that turns the \link[=rcds-anti-patterns]{anti-pattern catalog}
(RCDS.md section 8) into a mechanical check. It reads a script (or a string of
code) and flags the recurring mistakes from the archive -- deprecated \verb{size=}
on \code{geom_sf}, rainbow palettes on ordered data, hardcoded absolute font paths,
inline API keys, caption/signature drift, copy-pasted theme blocks, magic font
sizes, and non-hygienic exports -- each tied to its anti-pattern code and fix.
}
\details{
This is intentionally heuristic (regex over source, not a full parse): it errs
toward surfacing things to look at. Treat findings as prompts, not verdicts.
}
\examples{
code <- '
ggplot(d) + geom_sf(aes(fill = v), size = 0.2) +
  scale_fill_distiller(palette = "Spectral")
ggsave("m.png", width = 10, height = 8)
'
rcds_lint(code)
}
