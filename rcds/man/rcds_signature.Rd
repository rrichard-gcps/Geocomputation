% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-components.R
\name{rcds_signature}
\alias{rcds_signature}
\title{Build the canonical RCDS signature caption}
\usage{
rcds_signature(
  challenge,
  author = "Roland Richard",
  tool = "R",
  sources = NULL,
  handle = NULL
)
}
\arguments{
\item{challenge}{Top line / project tag, e.g. \code{"#30DayMapChallenge 2024 Day 16: Choropleth"}.}

\item{author}{Creator name. Default \code{"Roland Richard"}.}

\item{tool}{Tool line. Default \code{"R"}.}

\item{sources}{Character vector of data sources (one per line).}

\item{handle}{Optional social handle, appended to the author line.}
}
\value{
A single string with \verb{\\n} separators, ready for \code{labs(caption=)}.
}
\description{
The archive's single most consistent motif is the credit block:
\verb{#30DayMapChallenge 2024 Day N: Theme / Tool: R / Created By: Roland Richard / Data Sources: ...}. It drifts in format from map to map (pipes vs newlines,
"Tool" vs "Tools"). This builds it once, canonically, as a plain string you
can drop into \code{labs(caption=)}, or as rich text via \code{\link[=rcds_credits]{rcds_credits()}}.
}
\examples{
rcds_signature(
  challenge = "#30DayMapChallenge 2024 Day 16: Choropleth",
  sources = c("U.S. Census Bureau", "NCES")
)
}
