% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-theme.R
\name{theme_rcds}
\alias{theme_rcds}
\title{The RCDS theme}
\usage{
theme_rcds(base = 11, canvas = "dark", legend_position = "bottom")
}
\arguments{
\item{base}{Base type size in points (feeds \code{\link[=rcds_type_scale]{rcds_type_scale()}}). Use ~11 for
journal/screen, ~16-22 for posters under showtext.}

\item{canvas}{One of \code{"dark"}, \code{"deep"}, \code{"slate"}, \code{"graphite"}, \code{"light"},
\code{"vintage"}.}

\item{legend_position}{\code{"bottom"} (house default), \code{"right"}, \code{"none"}, or a
numeric \code{c(x, y)}.}
}
\value{
A ggplot2 theme object.
}
\description{
One theme to replace the ~40 hand-tuned \code{theme()} blocks in the archive.
\code{theme_rcds()} is the general chrome (titles, captions, legend, panel);
\code{\link[=theme_rcds_map]{theme_rcds_map()}} strips axes/grid for actual maps. Both read fonts via
\code{\link[=rcds_font]{rcds_font()}} and colours via \code{\link[=rcds_tokens]{rcds_tokens()}}, so changing the active voice
or a token restyles everything.
}
\details{
Canvas variants are lifted from the archive's recurring backgrounds:
\code{"dark"} (\verb{#1C1C1C}), \code{"deep"} (\verb{#121212}), \code{"slate"} (\verb{#2C3E50}),
\code{"graphite"} (\verb{#2B2B2B}), \code{"light"} (\verb{#F0F0F0}), \code{"vintage"} (\verb{#F5DEB3}).
}
\examples{
\dontrun{
ggplot(df) + geom_sf() + theme_rcds(canvas = "dark")
}
}
