% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-brand.R
\name{theme_map}
\alias{theme_map}
\title{The house map theme for the active brand}
\usage{
theme_map(base = 14, legend_position = "bottom", ...)
}
\arguments{
\item{base}{Base type size (points).}

\item{legend_position}{\code{"bottom"} (default), \code{"right"}, or \code{"none"}.}

\item{...}{Passed to the underlying theme.}
}
\value{
A ggplot2 theme.
}
\description{
The single entry point for a themed map: returns the active brand's default
map theme (GCPS Civic, or the archive dark theme). Switch the whole tool's
look with \code{\link[=rcds_brand]{rcds_brand()}} rather than changing every map.
}
\examples{
\dontrun{
ggplot(districts) + geom_sf(aes(fill = v)) + scale_fill_map_c() + theme_map()
}
}
