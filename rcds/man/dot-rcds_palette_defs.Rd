% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-palettes.R
\name{.rcds_palette_defs}
\alias{.rcds_palette_defs}
\title{RCDS palette families}
\usage{
.rcds_palette_defs()
}
\description{
A curated set of palette \emph{families} rather than one-off colour vectors. The
archive leaned on saturated rainbow scales (Spectral, ggsci futurama) that
are neither colourblind-safe nor print-stable. RCDS replaces them with
perceptually-ordered, accent-anchored ramps that hold up on the dark canvas,
in greyscale, and for the most common colour-vision deficiencies.
}
\details{
Families:
\describe{
\item{sequential}{\code{seq_blue}, \code{seq_amber}, \code{seq_teal} — single-hue luminance
ramps anchored on the brand accents.}
\item{diverging}{\code{div_balance} (amber<->blue, neutral mid), \code{div_temp}
(teal<->red).}
\item{qualitative}{\code{qual_brand} (6 brand-derived hues, max-distinct),
\code{qual_soft} (muted, for many categories on dark).}
\item{bivariate}{\code{biv_dkblue} 3x3 reference grid (drop-in for \code{biscale}).}
}

All ramps are designed to be passed through \code{\link[colorspace:darken]{colorspace::darken()}}/\code{lighten()}
if a specific canvas needs more contrast.
}
\keyword{internal}
