% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-interactive.R
\name{rcds_interactive_css}
\alias{rcds_interactive_css}
\title{RCDS CSS for interactive maps}
\usage{
rcds_interactive_css(canvas = "dark")
}
\arguments{
\item{canvas}{Canvas key (see \code{\link[=theme_rcds]{theme_rcds()}}).}
}
\value{
A single CSS string.
}
\description{
Generates the stylesheet that gives Leaflet (and other HTML map widgets) the
RCDS identity: token-driven CSS custom properties, the active font voice, and
styled popups, tooltips, controls, legend, and attribution on the chosen
canvas.
}
\details{
The \verb{:root \{ --rcds-* \}} custom properties are the \strong{integration seam}: a
Claude Design import (or any external design system) only needs to override
these variables to restyle every interactive map, exactly as overriding
\code{\link[=rcds_tokens]{rcds_tokens()}} restyles every static map.
}
\examples{
substr(rcds_interactive_css("dark"), 1, 60)
}
