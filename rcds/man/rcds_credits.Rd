% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcds-components.R
\name{rcds_credits}
\alias{rcds_credits}
\title{Signature as a rich-text credits panel (patchwork-ready)}
\usage{
rcds_credits(
  challenge,
  author = "Roland Richard",
  tool = "R",
  sources = NULL,
  handle = NULL,
  canvas = "deep",
  base = 11,
  align = c("left", "right")
)
}
\arguments{
\item{challenge}{Top line / project tag, e.g. \code{"#30DayMapChallenge 2024 Day 16: Choropleth"}.}

\item{author}{Creator name. Default \code{"Roland Richard"}.}

\item{tool}{Tool line. Default \code{"R"}.}

\item{sources}{Character vector of data sources (one per line).}

\item{handle}{Optional social handle, appended to the author line.}

\item{canvas}{Canvas key (see \code{\link[=theme_rcds]{theme_rcds()}}); the strip fill.}

\item{base}{Base type size in points.}

\item{align}{\code{"left"} or \code{"right"}.}
}
\value{
A ggplot object (a one-panel credits strip).
}
\description{
The \code{ggtext::geom_richtext} credit strip the archive builds by hand (Days 2,
16). Returns a standalone ggplot you can slot into \code{\link[=rcds_compose]{rcds_compose()}} or
\code{patchwork}. Falls back to a plain \code{geom_text} panel if \code{ggtext} is absent.
}
